<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.cop290.ashwattha.assn2.iitdcomplaints.Login">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/splash_imageView"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:adjustViewBounds="false"
        android:scaleType="centerCrop"
        android:src="@drawable/minimal_background"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/splash_imageView"
        android:layout_alignTop="@+id/splash_imageView"
        android:layout_alignRight="@+id/splash_imageView"
        android:gravity="center"
        android:layout_marginTop="40dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/login_screen_Text1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/splash_imageView"
            android:layout_alignRight="@+id/splash_imageView"
            android:gravity="center"
            android:text="IITD"
            android:textSize="80dp"
            android:textColor="@android:color/white"
            />

        <TextView
            android:id="@+id/login_screen_Text2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/splash_imageView"
            android:layout_alignRight="@+id/splash_imageView"
            android:gravity="center"
            android:text="Complaints System"
            android:textSize="50dp"
            android:textColor="@android:color/white"
            />

    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>